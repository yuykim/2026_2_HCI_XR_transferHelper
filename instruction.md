# XR 기반 실내 길찾기 프로젝트 정리

## 1. 개발 의도

이 프로젝트의 목표는 **XR을 활용해 실내 길찾기를 더 직관적으로 만드는 것**이다. 기존 네비게이션은 2D 지도나 텍스트 안내에 의존하기 때문에 사용자가 현재 위치와 이동 방향을 바로 이해하기 어렵다. 반면 XR은 현실 공간 위에 가상 경로를 직접 겹쳐 보여줄 수 있어, 사용자가 “어디로 가야 하는지”를 더 쉽게 파악할 수 있다. 

특히 이 프로젝트는 야외가 아니라 **건물 내부의 소규모 3차원 공간**을 대상으로 한다. 따라서 GPS보다는 **현장 기준점 기반 정렬과 재보정 방식**이 더 적합하다고 판단했다.

---

## 2. 기기 및 개발 환경 선택

### Meta Quest 3 선택 이유

Meta Quest 3는 완전한 일상형 XR 글래스는 아니지만, 현재 단계에서 passthrough 기반 XR 경험을 구현하기에 적합한 장비다.

핵심 이유는 다음과 같다.

* 현실 공간 위에 가상 경로를 띄울 수 있는 passthrough 지원
* 헤드 트래킹과 컨트롤러 입력 지원
* Unity와의 개발 연동이 비교적 안정적
* XR 길찾기 PoC를 빠르게 구현하기에 현실적인 선택

즉, Quest 3는 **“현실을 보면서 길안내를 받는 XR 서비스”**를 실험하기에 적절한 하드웨어다.

### Unity 선택 이유

Unity는 단순히 익숙해서가 아니라, **확장성과 개발 생산성** 때문에 선택했다.

* route 오브젝트 배치 가능
* 정렬 로직 구현 가능
* World Space Canvas UI 구성 가능
* Quest 빌드 설정 가능
* 향후 모바일 AR, XR 글래스, Apple Vision 계열 디바이스로 확장 가능

따라서 Unity는 현재 구현뿐 아니라 이후 플랫폼 확장까지 고려한 개발 도구라고 볼 수 있다.

---

## 3. 기술 스택

현재 프로젝트의 핵심 기술 스택은 다음과 같다.

| 기술                     | 역할                          |
| ---------------------- | --------------------------- |
| Unity                  | 전체 XR 앱 개발 환경               |
| Meta Quest 3           | XR/MR 실행 디바이스               |
| OpenXR                 | XR 기기와 Unity를 연결하는 표준 인터페이스 |
| Meta XR SDK            | Quest 전용 기능과 입력/UI 호환성 보강   |
| XR Interaction Toolkit | XR 상호작용 구조 구현               |
| Input System           | 입력 처리                       |

정리하면,
**OpenXR은 확장성을 위한 표준 기반**,
**Meta XR SDK는 Quest에서 안정적으로 동작하게 만드는 보강 도구**다.

---

## 4. 현재까지 구현한 것

현재까지는 **가상 경로를 만들고, 특정 기준점에 맞춰 XR 공간을 정렬하는 단계**까지 구현했다.

구현된 주요 내용은 다음과 같다.

* 기본 route 생성
* `NavigationRoot`를 `AlignTarget`에 맞춰 재배치
* 버튼 입력을 통한 정렬 동작
* 시선 dwell 방식 정렬 시도
* VR/XR 환경에서 UI 입력이 동작하도록 보강
* 가상 경로를 원하는 위치에 띄우는 데모 구현

즉, 현재 단계는 **“가상 경로를 특정 위치에 맞춰 배치하는 XR 길안내 데모”** 수준까지 완성된 상태다.

---

## 5. 현재 문제점

가장 큰 문제는 **현실 공간과 XR 공간의 정합이 계속 안정적으로 유지되지 않는다는 점**이다.

Meta Quest 3가 인식한 공간 기준과 실제 공간 기준이 완전히 일치하지 않을 수 있고, 사용자가 이동하면서 초기 정렬이 점점 어긋날 수 있다.

특히 문제가 되는 상황은 다음과 같다.

* Quest가 인식한 공간 범위를 벗어나는 경우
* tracking 기준이 흔들리는 경우
* 장거리 이동 중 오차가 누적되는 경우
* 초기 정렬 이후 재보정이 없는 경우

이 경우 가상 경로가 실제 위치와 맞지 않게 되어, 길안내 기능의 신뢰도가 떨어진다.

---

## 6. 해결 방향

GPS도 고려했지만, 실내에서는 정밀도가 부족하고 층이나 높이 정보를 제대로 다루기 어렵다. 그래서 GPS 대신 다음 방식이 더 적합하다고 판단했다.

### 대안 1. QR 코드 기반 재정렬

사용자가 특정 지점에서 QR 코드를 바라보면, 그 QR 코드를 기준으로 route를 다시 정렬한다.

### 대안 2. 특정 사물 인식

문, 표지판, 기둥 등 실내의 고정된 사물을 기준점으로 삼아 XR 공간을 다시 맞춘다.

### 대안 3. 장면 인식

공간의 시각적 특징을 인식해 사용자의 위치를 재보정한다.

### 대안 4. 구간별 route 분할

전체 경로를 한 번에 고정하지 않고, 복도·코너·분기점마다 구간을 나누어 route를 다시 불러온다.

---

## 7. 확장 가능성

현재는 단순 정렬과 route 배치 데모에 가깝지만, 이후에는 실제 서비스 형태로 확장할 수 있다.

### 단기 확장

* 방향 안내 UI 추가
* 거리 정보 표시
* 경로 이탈 경고
* 정렬 필요 여부 표시

### 중기 확장

* QR/마커 기반 재정렬
* 구간별 route 연결
* 긴 경로에서 누적 오차 감소
* 경로 이탈 시 재탐색 기능

### 장기 확장

* 스마트폰 AR 버전으로 전환
* XR 글래스 상용화 시 플랫폼 이전
* Apple Vision 계열 디바이스 확장
* 건물 단위 실내 내비게이션 시스템 연동

---

## 8. 발표용 한 문단 정리

우리 팀은 기존 2D 네비게이션보다 더 직관적인 실내 길찾기 경험을 제공하기 위해 XR 기반 실내 길안내 서비스를 기획했다. Meta Quest 3와 Unity를 활용해 현실 공간 위에 가상 경로를 띄우고, 특정 기준점에 맞춰 XR 공간을 정렬하는 기능까지 구현했다. 기술적으로는 OpenXR을 통해 확장 가능한 표준 기반을 확보하고, Meta XR SDK를 통해 Quest 환경에서의 입력과 UI 기능을 보강했다. 현재는 route 생성과 정렬 데모 단계까지 구현했지만, 사용자가 이동하면서 현실 공간과 가상 경로가 어긋나는 문제가 확인되었다. 이를 해결하기 위해 QR 코드, 특정 사물, 장면 인식 기반의 재정렬 방식과 구간별 route 분할 방식을 고려하고 있으며, 향후 실제 실내 XR 길안내 서비스로 확장할 수 있다.